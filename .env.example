# 数据库连接字符串
# SQLite 数据库文件路径
DATABASE_URL="file:./dev.db"

# 如果使用 PostgreSQL，使用以下格式:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# 如果使用 MySQL，使用以下格式:
# DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/DATABASE"
